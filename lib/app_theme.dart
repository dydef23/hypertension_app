import 'package:flutter/material.dart';

class AppTheme {
  static final String appTitle = 'Text Classification';
  static final ThemeData theme = ThemeData(
    colorScheme: ColorScheme.fromSeed(seedColor: Colors.deepPurple),
    useMaterial3: true,
  );
}
