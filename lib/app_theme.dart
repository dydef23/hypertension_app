import 'package:flutter/material.dart';

class AppTheme {
  Color customColor = Color(0xFFDC2738);
  static final String appTitle = 'Hypertension Prediction';
  /*static final ThemeData theme = ThemeData(
    colorScheme: ColorScheme.fromSeed(seedColor: Colors.deepPurple),
    useMaterial3: true,
  );*/
  static final ThemeData theme = ThemeData(
    primaryColor: Color(0xDC2738), // Ganti dengan kode HEX yang diinginkan
    useMaterial3: true,
  );
}
